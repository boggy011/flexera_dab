# QA/Stage Environment Configuration
# ============================================================================
# NOTE: This file contains NON-SENSITIVE configuration only!
# 
# Sensitive values (CLIENT_ID, CLIENT_SECRET) should be set via:
#   - GitHub Secrets (for CI/CD)
#   - Local environment variables (for local development)
#
# The GitHub Actions workflow automatically injects secrets as env vars.
# ============================================================================

# Databricks workspace URL (non-sensitive, can be committed)
export DATABRICKS_HOST="${DATABRICKS_HOST:-https://adb-yyyy.azuredatabricks.net}"

# Service Principal credentials - DO NOT hardcode here!
# These will be injected by GitHub Actions from secrets:
#   - DATABRICKS_CLIENT_ID (from GitHub Secret: DATABRICKS_CLIENT_ID_QA)
#   - DATABRICKS_CLIENT_SECRET (from GitHub Secret: DATABRICKS_CLIENT_SECRET_QA)

# Catalog configuration
export CATALOG_NAME="qa_catalog"

# Storage paths
export LANDING_ZONE_PATH="/mnt/landing/qa"
export CHECKPOINT_PATH="/mnt/checkpoints/qa"

# Cluster configuration
export JOB_CLUSTER_NODE_TYPE="Standard_DS4_v2"
export JOB_CLUSTER_NUM_WORKERS="2"

# Optional: Interactive cluster ID (if using existing cluster)
# export INTERACTIVE_CLUSTER_ID="yyyy-yyyyyy-yyyyyyyy"

# Notifications
export NOTIFICATION_EMAIL="qa-team@company.com"
