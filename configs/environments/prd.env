# Production Environment Configuration
# ============================================================================
# NOTE: This file contains NON-SENSITIVE configuration only!
# 
# Sensitive values (CLIENT_ID, CLIENT_SECRET) should be set via:
#   - GitHub Secrets (for CI/CD)
#   - Local environment variables (for local development)
#
# The GitHub Actions workflow automatically injects secrets as env vars.
# ============================================================================

# Databricks workspace URL (non-sensitive, can be committed)
export DATABRICKS_HOST="${DATABRICKS_HOST:-https://adb-zzzz.azuredatabricks.net}"

# Service Principal credentials - DO NOT hardcode here!
# These will be injected by GitHub Actions from secrets:
#   - DATABRICKS_CLIENT_ID (from GitHub Secret: DATABRICKS_CLIENT_ID_PRD)
#   - DATABRICKS_CLIENT_SECRET (from GitHub Secret: DATABRICKS_CLIENT_SECRET_PRD)

# Catalog configuration
export CATALOG_NAME="prd_catalog"

# Storage paths
export LANDING_ZONE_PATH="/mnt/landing/prd"
export CHECKPOINT_PATH="/mnt/checkpoints/prd"

# Cluster configuration
export JOB_CLUSTER_NODE_TYPE="Standard_DS5_v2"
export JOB_CLUSTER_NUM_WORKERS="4"

# Optional: Interactive cluster ID (if using existing cluster)
# export INTERACTIVE_CLUSTER_ID="zzzz-zzzzzz-zzzzzzzz"

# Service principal name for run_as in DAB
export DATABRICKS_SP_NAME="sp-datalib-prd"

# Notifications
export NOTIFICATION_EMAIL="data-engineering@company.com"
