# Development Environment Configuration
# ============================================================================
# NOTE: This file contains NON-SENSITIVE configuration only!
# 
# Sensitive values (CLIENT_ID, CLIENT_SECRET) should be set via:
#   - GitHub Secrets (for CI/CD)
#   - Local environment variables (for local development)
#
# The GitHub Actions workflow automatically injects secrets as env vars.
# ============================================================================

# Databricks workspace URL (non-sensitive, can be committed)
export DATABRICKS_HOST="${DATABRICKS_HOST:-https://adb-xxxx.azuredatabricks.net}"

# Service Principal credentials - DO NOT hardcode here!
# These will be injected by GitHub Actions from secrets:
#   - DATABRICKS_CLIENT_ID (from GitHub Secret: DATABRICKS_CLIENT_ID_DEV)
#   - DATABRICKS_CLIENT_SECRET (from GitHub Secret: DATABRICKS_CLIENT_SECRET_DEV)

# Catalog configuration
export CATALOG_NAME="dev_catalog"

# Storage paths
export LANDING_ZONE_PATH="/mnt/landing/dev"
export CHECKPOINT_PATH="/mnt/checkpoints/dev"

# Cluster configuration
export JOB_CLUSTER_NODE_TYPE="Standard_DS3_v2"
export JOB_CLUSTER_NUM_WORKERS="1"

# Optional: Interactive cluster ID (if using existing cluster)
# export INTERACTIVE_CLUSTER_ID="xxxx-xxxxxx-xxxxxxxx"

# Notifications
export NOTIFICATION_EMAIL="dev-team@company.com"
